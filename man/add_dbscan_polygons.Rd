% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{add_dbscan_polygons}
\alias{add_dbscan_polygons}
\title{Automatically detect cancer cell regions and create polygons}
\usage{
add_dbscan_polygons(
  seurat_obj,
  fov_name = "fov",
  cancer_col = "is_cancer",
  eps = 35,
  minPts = 20,
  concavity = 2,
  length_threshold = 0
)
}
\arguments{
\item{seurat_obj}{Seurat object with spatial coordinates}

\item{fov_name}{Name of the field of view (default: "fov")}

\item{cancer_col}{Metadata column indicating cancer cells (default: "is_cancer")}

\item{eps}{DBSCAN epsilon parameter - max distance between points (default: 35)}

\item{minPts}{DBSCAN minimum points per cluster (default: 20)}

\item{concavity}{Concave hull concavity parameter, lower = tighter fit (default: 2)}

\item{length_threshold}{Edge length threshold for concave hull (default: 0)}
}
\value{
Seurat object with cancer region polygons added to FOV
}
\description{
Uses DBSCAN clustering on cancer cell coordinates to identify spatial regions,
then creates concave hull polygons around each cluster. Adds polygons to the
Seurat object's FOV.
}
\examples{
\dontrun{
seurat_obj <- add_dbscan_polygons(seurat_obj, eps = 50, minPts = 30)
}

}
