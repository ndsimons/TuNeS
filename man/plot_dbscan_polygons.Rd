% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_dbscan_polygons}
\alias{plot_dbscan_polygons}
\title{Plot DBSCAN-based cancer region polygons}
\usage{
plot_dbscan_polygons(
  seurat_obj,
  fov = "fov",
  eps = 35,
  minPts = 20,
  concavity = 2,
  length_threshold = 0,
  sample_name = NULL,
  plot = TRUE
)
}
\arguments{
\item{seurat_obj}{Seurat object with spatial coordinates}

\item{fov}{Name of the field of view (default: "fov")}

\item{eps}{DBSCAN epsilon parameter - max distance between points (default: 35)}

\item{minPts}{DBSCAN minimum points per cluster (default: 20)}

\item{concavity}{Concave hull concavity parameter, lower = tighter fit (default: 2)}

\item{length_threshold}{Edge length threshold for concave hull (default: 0)}

\item{sample_name}{Sample name for plot title}

\item{plot}{Whether to generate visualization plots (default: TRUE)}
}
\value{
sf object containing polygon geometries for each cancer region
}
\description{
Uses DBSCAN clustering on cancer cell coordinates to identify spatial regions,
creates concave hull polygons around each cluster, and optionally visualizes them.
}
\examples{
\dontrun{
# Basic usage
polygons <- plotDBSCANpolygons(seurat_obj, sample_name = "Patient1")

# Custom parameters without plotting
polygons <- plotDBSCANpolygons(
  seurat_obj, 
  eps = 50, 
  minPts = 30,
  sample_name = "Patient1",
  plot = FALSE
)
}

}
